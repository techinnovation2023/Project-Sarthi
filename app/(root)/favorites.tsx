import { Text, View } from "react-native"
import { SafeAreaView } from "react-native-safe-area-context";

const Favorites = () => {
    return (
        <SafeAreaView>
            <Text>Favorites Page</Text>
            <Text>Save Your Favorite Location Here</Text>
        </SafeAreaView>
    )
};

export default Favorites;
